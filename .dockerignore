# AITester .dockerignore
# Dockerfile 的 `COPY . .` 会把构建上下文中未被忽略的文件打进镜像。
# 这里排除所有敏感 / 无关 / 体量大的内容，尤其是本地含真实 LLM 密钥的 .env 文件。

# ── 版本控制 ──────────────────────────────────────────────────────────────────
.git
.gitignore

# ── 虚拟环境（镜像内按 requirements 重新安装，无需带入）────────────────────────
.venv
venv
env
venv_old

# ── 本地敏感配置（真实 LLM 密钥 / DB 凭证，严禁打进镜像）─────────────────────
# 镜像运行时由用户挂载自己的 .env.local（见 Dockerfile 注释），不需要内置这些
.env
.env.*
config.local.example

# ── 私有隔离目录（论文素材 / 实验原始数据，永不外泄）─────────────────────────
.private
.local

# ── 测试 / lint 缓存与覆盖率产物 ─────────────────────────────────────────────
.pytest_cache
.ruff_cache
__pycache__
*.pyc
.coverage
coverage.xml
htmlcov

# ── 运行时日志与本地 LLM 缓存 ────────────────────────────────────────────────
*.log
aitester.log
src/cache
rag_data

# ── 实验输出（本地数据，体积大且不含可复现代码）──────────────────────────────
experiments/results

# ── IDE / 系统噪音 ───────────────────────────────────────────────────────────
.idea
.vscode
.DS_Store
