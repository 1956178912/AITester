# 0.7 轮次性能再审计发现清单（B 方向）

**审计日期**：2026-09-20
**审计范围**：`src/` 全模块 + `experiments/` + 文档（只读，未改动任何 `src/` 代码）
**基线实测**（审计当时工作区状态，含 6 个未提交文件）：
- `pytest tests/` 全量：**1625 passed / 0 failed**（收集 1625）
- `ruff check .`：**1 条告警**（`tests/test_cross_file.py:423` RUF059）——非"全绿"
- src 总覆盖率：94%（按 0.6 基线；本轮未重跑 cov）

> ⚠️ 工作区现状说明：HEAD 提交（`01ddf76` docs 同步）之后，本地又有一批 0.7 轮次
> 的未提交改动（`src/graph/nodes.py`、`src/tools/cross_file.py`、`src/tools/dependency.py`、
> 3 个测试文件，共 +608/-30 行，主题：跨文件修复二期多入口分析 + `getctime→getmtime`
> 口径修正）。本审计以"当前工作区实际代码"为准；该未提交批次自身的问题也一并记录在案。
>
> 跳过项（0.6 已记录留待债务，本轮不复审计）：P1-3 pytest 重试去抖、P1-4 RAG 容量满
> 全表扫描节流、P1-5 executor_imports LRU key 归一。

---

## 路线 1：死代码 / 技术债扫描

| # | 位置 | 类别 | 严重度 | 说明 | 建议动作 |
|---|------|------|--------|------|----------|
| 1.1 | `tests/test_cross_file.py:423` | 静态检查告警 | **P0** | RUF059：`new_files, ok = apply_multi_file_patch(...)` 中 `new_files` 从未使用。这是 0.6 之后唯一残留的 ruff 告警，使"ruff 全绿"基线不再成立（README/CHANGELOG 均承诺 Ruff 全绿）。 | `new_files` 改为 `_`（保留断言语义）或补一条对 `new_files` 的断言；一行修复。 |
| 1.2 | `src/tools/cross_file.py:347-348` | 格式 | **P0** | 未提交批次遗留：`analyze_multi_entry_deps` 末尾 `return sorted(...)` 与紧随的 `def _collect_imported_symbols` 之间**缺失一个空行**（PEP8 要求顶层定义间隔 2 行）——`ruff format --check` 会在此失败。 | 补空行；并入 0.7 批次提交前跑一次 `ruff format`。 |
| 1.3 | `src/graph/llm_cache.py`（整模块 201 行） | 双套缓存并存 | **P1** | 进程内 LRU 缓存模块（`cached_llm_call` 装饰器 + `get_cached_response`/`set_cached_response` 手动接口 + `clear_cache`），生产调用链上**零使用**——`BaseAgent._call_llm_with_cache` 走的是另一套**文件缓存**（`src/cache/*.json`，`_llm_cache_dir` 口径），LLM 调用根本不经过 llm_cache 模块。全仓仅 `tests/test_llm_cache.py` 单测 + `workflow.get_workflow_stats()` 引用（读它的统计打报告用）。两套缓存互不相通，属于历史债。 | 记录在案：要么把文件缓存并入 llm_cache（统一口径），要么在 `llm_cache` 模块 docstring 顶部显式标注"生产路径已改用文件缓存，本模块仅保留接口供嵌入式使用"。不做删除（单测覆盖了它，且 `get_workflow_stats` 消费其统计）。 |
| 1.4 | `src/agents/base_agent.py:254-266` | 死委托 | **P2** | `BaseAgent._find_balanced_json` staticmethod 纯粹转发 `src.utils.helpers._find_balanced_json`，全仓无调用点（`extract_json_object` 内部已直接消费该 helper；`_extract_json`/`_extract_python_code` 有调用，唯独它没有）。0.6 拆分时保留的"历史命名空间转发"，但未使用方比 1.3 的更纯粹（连测试都不 import 它）。 | 记录在案；若后续清理死委托统一做（含确认 `src.graph.workflow` 的 re-export 中是否有等价项）。 |
| 1.5 | `src/agents/executor.py:207-219` | 类属性挂载 | **P2** | 模块函数经 `ExecutorAgent.xxx = f` 批量挂载为类属性/staticmethod（13 处），保持历史 patch 路径 `src.agents.executor.ExecutorAgent.<method>` 不变。属刻意设计（拆分轮次的兼容层），非死代码，但属"为测试便利牺牲可读性"的债务点。 | 记录在案，不动。 |
| 1.6 | `experiments/analyze_results.py`（26 个 `_` 前缀统计函数） | 模块膨胀 | **P2** | 单文件 1300+ 行、26 个私有统计函数 + 渲染函数堆叠。各函数间耦合低（都只消费 `details[]`），可按主题拆子模块（rag_* / convergence_* / contamination_* 三组），但不影响正确性。 | 记录在案；无性能问题（单次全表遍历 O(n)），仅在需要时拆。 |

**死代码扫描方法说明**：`rg "def _xxx"` 候选 157 个（src 107 + experiments 50）逐一检查引用，
未使用 import / 恒真恒假表达式 / `pass` 空体均为 0 命中（0.6 已清零，本轮确认无回潮）。
唯一真实的"未引用"类问题就是 1.4。

---

## 路线 2：性能热点

| # | 位置 | 类别 | 严重度 | 说明 | 建议动作 |
|---|------|------|--------|------|----------|
| 2.1 | `src/rag/retriever.py:235`（`_upsert` 写锁）+ `graph/nodes.py:264-278, 502-513` | **--parallel 串行化瓶颈** | **P1** | RAG 单例 `TestCaseRetriever` 的 `_write_lock` 把「全表清理 + 容量检查 + `collection.upsert`」整段串行化。`upsert` 内含**嵌入模型推理 + HNSW 写入**（单次 ~50-200ms，视文档长度），而它在 `add_case`/`add_repair` 热路径上。`--parallel=4` + `ENABLE_RAG=true` 时，所有 worker 的 RAG 入库互相排队：每任务 ≥2 次 add（executor 的 add_case + debugger 的 add_repair），单任务多付 3×(50-200ms)=150-600ms 串行等待；50 任务 × 3 基线场景累计 **数分钟级**。注意 P1-4（容量满全表扫描节流）只覆盖了"读/清理"侧，**写锁整段串行化是另一独立问题，0.6 未处理**。 | 量化方案：把清理 + 容量检查挪到锁外（`count()` 本身线程安全），锁内只做 `upsert`；或把 add 改为「批量缓冲 + 单写线程」模型（worker 只 append 到队列，后台线程统一 upsert，彻底消除锁竞争）。目标：`--parallel` + RAG 场景 P99 入库等待从 O(N×embed_ms) 降至 <10ms。 |
| 2.2 | `src/agents/llm_client.py:167-189`（`_retry_with_exponential_backoff`） | 重试放大 | **P2** | 0.6 P0-1 后，`_call_llm` 外层故障转移循环（每个 base_url 组内每个模型）× 内层 4 次尝试（1s/2s/4s 退避）：最坏情形 = `组数 × 模型数 × (4 次调用 + 7s 退避)`。配 3 组 × 2 模型时单调用最坏 ~3×2×(LLM_TIMEOUT×4+7s)——若 LLM_TIMEOUT=60s 即 **~48min 全失败路径**（此前 P0-1 只解决了"一次抖动即失败"，没有引入跨 API 的**总预算上限**）。 | 记录在案：在 `_call_llm` 加一个全局墙钟预算（如 `LLM_CALL_BUDGET_SECONDS=600`，超出即快速失败），防止极端组合下基准跑批被单个任务卡死。 |
| 2.3 | `experiments/analyze_failures.py:334-349`（`load_all_results`） | 全表遍历 | **P2** | 遍历 `results_dir` 全部 `benchmark_*.json`，**每个任务全行 load + 全行 append 到内存**（`all_tasks` 携带完整 `generated_test`/`test_output`/`execution_trace` 大字段）。50 任务 × 3 基线 × 多批次累积后（如 10 个批次 = 1500 行），内存常驻 ~数百 MB；且分析函数（root_cause 等）对 `details` 多次全表遍历（每个 `_xxx` 函数各扫一遍）。 | 记录在案：改为按需读取 + 字段投影（只取 `task_id/passed/error_category/diagnosis/patch` 等分析字段），或给 analyze_failures 加 `--limit` 参数。当前数据量（单批次）下无实际痛感。 |
| 2.4 | `src/tools/dependency.py:387-395`（`_record_venv_cache_event`） | 双锁临界区 | **P2** | 0.6 P0-2 双锁分离后复核：计数锁临界区确实只有内存累计（ns 级），`--parallel` 热路径不排队——**修复有效**。但 `_persist_cache_stats` 在 `_venv_cache_persist_lock` 内做 `json.load` + `os.makedirs` + `json.dump`（~2-5ms/事件，且**每次** hit/create 事件都触发一次落盘）：N 任务串行跑完 = N 次小文件随机写（venv 命中场景 N=任务数×轮次，常 100-300 次）。单条 2-5ms 无感，但 300 事件累计 0.6-1.5s 纯 IO，且全部在 worker 线程同步路径上。 | 记录在案：加节流（如距上次落盘 <5s 时只累计内存、由 `get_venv_cache_stats`/进程退出兜底落盘），可再省累计 ~1s。不动 P0（lost-update 语义已由双锁保证）。 |
| 2.5 | `experiments/run_benchmark.py:778-782`（并行派发） | 无界提交 | **P2** | `ThreadPoolExecutor(max_workers=parallel)` + 把全部任务一次性 `submit`（100+ 任务时未来列表 100+ 个，每个 future 持有 `task/baselines/...` 完整引用常驻内存）。worker 数有界、CPU 不受影响，但大对象常驻 + 长任务场景下内存峰值偏高。 | 记录在案：改 `as_completed` 滑窗（保持 ≤2×parallel 个在途 future）即可，收益有限。 |
| 2.6 | `src/graph/rag.py:43-88`（单例初始化） | 复核通过 | — | 复核：双重检查锁 + `_rag_init_failed` 短路后，嵌入模型初始化（ChromaDB 客户端 + bge-small-zh-v1.5 加载，~1-3s）确实只发生在进程首次 `get_rag_retriever()`，且初始化失败后全节点 no-op 不再重付 2-6s——**无新热点**，单例化收益在 0.6 后保持完整。 | 无动作（记录复核结论）。 |
| 2.7 | `src/tools/patch_applier.py`（多候选补丁 0.6 修复） | 复核通过 | — | 复核：`static_validate_patch` 3-tuple 契约生效，`generate_candidates` 对 N 候选（默认 3）的 `apply_patch_to_code` 双次应用确已消除——每候选省 1 次"正则 + 行范围定位 + 空行压缩"（~0.5-2ms），N=3 时每修复轮省 1.5-6ms。收益小但方向正确，无回潮。 | 无动作（记录复核结论）。 |

**性能热点扫描方法说明**：热路径（并行 worker、LLM 调用链、venv 统计、RAG 检索/入库、
分析层）逐文件通读 + 0.6 修复点逐个复核。2.1 是本轮新发现的唯一"显著可量化"热点；
2.2/2.4/2.5 为记录在案的次级项。

---

## 路线 3：文档漂移复查

| # | 位置 | 类别 | 严重度 | 说明 | 建议动作 |
|---|------|------|--------|------|----------|
| 3.1 | `README.md:12-13` + `CHANGELOG.md:50-51` + `docs/api_reference.md:6,716` | 数字漂移 | **P0** | 三处均写"全量 **1612** passed / ruff 全绿"。实测（审计时工作区）为 **1625 collected / 1625 passed**，且 ruff 残留 1 条 RUF059（见 1.1）。1612 是 0.6 提交时数字；0.7 未提交批次新增 13 个用例（`tests/test_cross_file.py` +284 行、`tests/test_dependency_edge_cases.py`、`tests/test_workflow_extended.py`）后未同步文档。 | 0.7 批次提交时三处数字统一更新为实测值（1625 / ruff 0 告警——**先修 1.1/1.2**），并在 CHANGELOG 0.7 节登记。 |
| 3.2 | `docs/code_analysis_report.md:13`（头部过期标注） | 标注过期 | **P1** | 头部 0.6 过期标注仍写"当前测试基线为 1612 passed"——按任务要求不改正文，但此标注本身已落后 13 个用例（实测 1625）。标注语义"历史快照、数字过期"仍正确，仅数字需在下轮更新头部。 | 记录在案：0.7 批次提交时在头部追加一行"0.7 基线已至 1625"（不改正文其余部分）。 |
| 3.3 | `.env.example` 全部承诺变量 | 复核通过 | — | 逐一核对：`TEMPERATURE`/`MYSQL_*`/`EXECUTION_TIMEOUT`/`EXECUTOR_USE_VENV`/`EXECUTOR_AUTO_INSTALL_DEPS`/`EXECUTOR_DEP_INSTALL_TIMEOUT`/`MAX_ITERATIONS`/`COVERAGE_THRESHOLD`/`ENABLE_PLANNER`/`ENABLE_RAG`/`RAG_PERSIST_PATH`/`RAG_COLLECTION_NAME`/`RAG_TTL_SECONDS`/`BENCHMARK_PARALLELISM`/`LLM_TIMEOUT`/`LLM_RETRY_WAIT` 均在 `config.py` 有 `os.getenv`/`_parse_*_env` 读取点；`AITESTER_VENV_CACHE_DIR` 由 `src/tools/dependency.py:90` 读取（非 config.py，合理——功能模块自读，与 0.6 收敛口径一致）。**无幽灵变量**。 | 无动作。 |
| 3.4 | `.env.example`（`AITESTER_LLM_CACHE` / `AITESTER_LLM_CACHE_DIR` 小节缺失） | 文档缺失 | **P2** | 该两个变量在 README/QUICKSTART 有文档（`README.md:151-156`、`QUICKSTART.md:79`），`src/agents/llm_client.py:389-396` 有读取点，但 `.env.example`（非敏感配置模板）没有列——与 `.env.example` 自身"本文件承载非敏感项"的定位相符（它确实是非敏感项，缺的是**列举**，不是**承诺断裂**）。 | 记录在案：0.7 批次补一行进 `.env.example` 的"工作流"小节即可。 |
| 3.5 | `docs/performance_guide.md:6`（最后更新 2026-09-18） | 日期漂移 | **P2** | 正文 10 章仍准确（RAG 单例/超时/并行/venv 缓存多版本等描述与代码一致），但"最后更新"日期停在 0.4 轮次，0.6 P0 修复（LLM 重试退避对齐、venv 双锁）未反映到"4.x 重试策略"章节——`2.4 重试策略` 伪代码仍写 `2**attempt * LLM_RETRY_WAIT` 旧口径（实际 0.6 后是 `base_wait * 2**attempt`，base_wait=1s，且 `LLM_RETRY_WAIT` 现仅用于 `run_benchmark` 限流等待，**不再驱动退避**）。 | 0.7 批次顺手改 2.4 节伪代码 + 更新日期。 |
| 3.6 | 根目录 `.env.local.template`（tracked） | 安全审查 | — | 该文件由 `generate_env_template()` 生成并**被 git 跟踪**（非 `.env`/`.env.local`，不受 .gitignore 保护），但内容为占位符（`your-aliyun-api-key-here` 等），**无真实密钥**，不触发脱敏流程。注意它与 `config.local.example` 功能重叠（两者都是"密钥模板"），`llm_configs.json` 的 `required_api_key` 又指向第三种命名（`ALIYUN_BAILIAN_API_KEY`）——三套密钥命名约定并存。 | 记录在案：三套命名收敛为 `config.local.example`（LLM_N_*）单一事实来源，其余标注"仅供批量脚本中间变量"。 |
| 3.7 | `QUICKSTART.md:8`（`git clone https://github.com/1956178912/AITester.git`） | 链接 | **P2** | 公开仓库地址。若项目实际为私有仓库或地址已变更，此处漂移（无法离线核验，仅记录）。 | 需人工确认仓库地址是否仍有效；无效则更新或删除 clone 示例。 |

**文档漂移扫描方法说明**：`.env.example` 每个承诺变量 → `config.py`/功能模块读取点逐一映射；
CHANGELOG/README/api_reference 数字 → 实测基线对比；`code_analysis_report.md` 仅核对头部标注
（按要求不改正文）；根目录敏感文件模板做占位符扫描（未检出真实密钥，无需脱敏标注）。

---

## 本轮实施建议

按任务口径：**P0 全做、P1 按优先级前 3 做、P2 不动**。

### P0（必须本轮修，共 3 条）
1. **1.1** `tests/test_cross_file.py:423` RUF059：`new_files` 改 `_` 或补断言（一行）。
2. **1.2** `src/tools/cross_file.py:347` 补 PEP8 空行（`ruff format` 一次归一）。
3. **3.1** README / CHANGELOG / api_reference 三处 1612 → 实测数字，并确认 ruff 0 告警
   （依赖 1.1/1.2 先落地）；CHANGELOG 补 0.7 节。

### P1（建议修，按优先级前 3，共 2 条实修 + 1 条顺手）
1. **2.1** RAG 写锁热路径：`_upsert` 锁内只保留 `upsert`，清理 + 容量检查移到锁外
   （`collection.count()` 线程安全）；预估 `--parallel=4` + RAG 场景每任务省 150-600ms
   串行排队（P99 入库等待 <10ms）。属本轮收益最大项。
2. **3.2** `code_analysis_report.md` 头部追加 0.7 基线行（1625），不改正文。
3. **1.3** `llm_cache` 模块 docstring 标注"生产路径已改用文件缓存"（消除双套缓存的
   认知漂移，零代码行为变化）。

### P2（记录在案，不动）
1.4、1.5、1.6（死委托 / 类属性挂载 / 分析层拆模块）；2.2（LLM 故障转移总预算）、
2.3（analyze_failures 字段投影）、2.4（venv 统计落盘节流）、2.5（并行提交滑窗）；
3.4（.env.example 补 AITESTER_LLM_CACHE 行）、3.5（performance_guide 2.4 节伪代码）、
3.6（三套密钥命名收敛）、3.7（clone 地址核验）。

### 额外提醒（非清单项，提请主代理注意）
- **工作区有 6 个未提交文件**（0.7 跨文件二期批次）：本审计发现的 1.1/1.2 均出自该批次，
  建议主代理提交前先把它们清掉，否则 "ruff 全绿" 门禁在 CI 上会挂。
- 实测基线（1625）与文档基线（1612）差 13 个用例，正好等于未提交批次新增用例数
  （`tests/test_cross_file.py` 等 3 文件）——**数字漂移 3.1 的根因就是这批未提交改动**，
  提交后同步文档即可自然对齐。
