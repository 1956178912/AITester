# AITester 研究动机与问题定义

## 1. 研究背景

自动化软件测试是软件工程领域的核心问题之一。传统静态分析工具（如 pylint、mypy）只能发现类型错误和语法问题，无法验证运行时行为；基于约束的测试生成工具（如 Pynguin、EvoSuite）依赖路径覆盖启发式搜索，对复杂业务逻辑的覆盖能力有限；而基于 LLM 的测试生成方法（如 TestLoter、CHATTESTER）虽能生成语义丰富的测试用例，但普遍缺乏对函数逻辑的系统性分析，导致测试用例质量不稳定、失败修复依赖随机重试。

## 2. 现有工具的局限性

### 2.1 测试生成质量低
- 直接 LLM 生成测试时，缺乏对输入域、边界条件和异常路径的结构化分析
- 测试用例之间缺乏逻辑关联，难以保证充分覆盖

### 2.2 自我修复能力弱
- 现有方法在测试失败后通常直接重新生成测试代码，未区分错误类型
- 语法错误、断言失败、运行时异常的修复策略应当不同，但现有工作统一处理

### 2.3 缺乏理论模型
- 多智能体协作流程多为工程实现，缺少形式化的状态转移描述
- 无法证明各模块对最终测试质量的贡献

## 3. 核心假设

**多智能体协作 + 显式逻辑分析 + 分层错误修复** 能够显著提升自动化测试的生成质量和修复效率。

具体而言：
- **逻辑驱动思维链**：Planner 先分析函数的输入/输出域和前置/后置条件，为 Generator 提供结构化的测试指导
- **分层错误修复**：将测试失败分类为语法、断言、运行时、超时四类，分别采用差异化的修复策略
- **多智能体迭代协作**：Planner → Generator → Executor → Debugger 的闭环流程，每轮迭代都有明确的状态转移

## 4. 研究问题（Research Questions）

### RQ1：逻辑驱动的思维链能否提升测试用例的覆盖率？
对比基线方法（直接 LLM 生成测试）与本方法（先进行逻辑分析再生成测试），在标准缺陷数据集上测量行覆盖率和分支覆盖率。

### RQ2：分层错误修复策略能否提高修复成功率？
对比统一修复策略（不分类别）与本方法的分类修复策略，测量修复成功率和平均修复轮数。

### RQ3：多智能体架构相比单智能体方案是否显著优于各子方法？
通过消融实验，分别移除 Planner（退化为直接生成）、Debugger（无修复循环）、逻辑分析模块，对比各变体的性能。

## 5. 预期贡献

1. 提出一种基于逻辑驱动思维链的多智能体测试生成框架
2. 设计分层错误修复机制，针对不同错误类型采用差异化策略
3. 在标准缺陷数据集上进行系统评估，验证方法有效性
4. 提供完整的可复现实验环境和代码
