# ─── 核心依赖 ─────────────────────────────────────────────────────────────────
# 版本锁定说明：以下顶层依赖使用 == 锁定到本地验证过的版本（与 requirements.lock 同步，2026-09-09）。
# 锁定目的：CI 跨 Python 3.12-3.14 安装时行为可复现，避免上游发版导致工作流行为漂移。
# 升级依赖时请同步更新 requirements.lock（在 .venv 中 pip freeze）并跑全量测试。
#
# langchain: LLM 应用开发框架，提供 Prompt 模板和消息管理
langchain==1.3.15
# langchain-openai: OpenAI API 适配器，支持 gpt-4o-mini 等模型
langchain-openai==1.4.3
# langgraph: 多智能体状态机编排框架，实现工作流有向图
langgraph==1.2.11
# pymysql: MySQL 数据库驱动，用于实验数据持久化
pymysql==1.2.0
# DBUtils: 数据库连接池（PooledDB），src/db/mysql_client.py 使用以提升并发能力
# （2026-09-09 依赖审计补齐：此前漏声明，全新环境安装本文件后 src.db 模块无法导入）
DBUtils==3.1.2
# click: 命令行接口框架，提供 aitester run 等子命令
click==8.4.2

# ─── 测试工具 ─────────────────────────────────────────────────────────────────
# pytest: Python 测试框架，运行单元测试
pytest==9.1.1
# pytest-cov: pytest 覆盖率插件，生成行覆盖率报告
pytest-cov==7.1.0
# pytest-timeout: pytest 超时控制插件（pyproject addopts 的 --timeout=1200 依赖它，CI 必装）
# 注：锁定 2.4.0。2.5.0 已被上游 yank（原因 "accidental breaking change (probably)"），
# 且 lock 曾误锁 2.5.0；2026-09-09 降级为最新未 yank 版本 2.4.0（已验证与 pytest 9.1.1 兼容）
pytest-timeout==2.4.0
# python-dotenv: 从 .env 文件加载环境变量
python-dotenv==1.2.2

# ─── UX 优化依赖 ─────────────────────────────────────────────────────────────
# rich: 富文本输出库，提供彩色输出、表格、进度条等
rich==15.0.0
# tqdm: 进度条库，显示任务执行进度
tqdm==4.70.0
# requests: HTTP 请求库，scripts/check_quota.py（LLM 额度探测）使用
requests==2.34.2
# chromadb: 向量数据库，RAG 模块依赖，用于存储检索历史测试用例
chromadb==1.5.9
# matplotlib: 图表绘制库，用于实验结果可视化
matplotlib==3.11.1
# pandas: 数据分析库，用于结果表格生成和统计
pandas==3.0.5

# ─── 实验与数据集依赖 ─────────────────────────────────────────────────────────
# datasets: HuggingFace 数据集加载库（SWE-bench 下载用）
datasets==5.0.1
# scipy: 统计检验（消融实验结果分析）
scipy==1.18.0

# ─── LLM 厂商 SDK ────────────────────────────────────────────────────────────
# zai: 智谱AI官方SDK，用于调用glm-4.7-flash等模型
zai-sdk==0.2.3

# ─── 已移除的依赖（记录备查）─────────────────────────────────────────────────
# - radon：代码复杂度分析工具，全项目无任何 import 引用（2026-09-09 依赖审计移除）。
#   若需手动分析被测代码复杂度，临时安装即可：pip install radon

# ─── 开发工具（按需安装，不进 CI）───────────────────────────────────────────
# ruff: 快速 lint 检查。CI 固定安装 ruff==0.16.3（与 requirements.lock 一致，
# 避免上游发版改变格式化规则导致 format 门禁随机转红）；本地开发工具，不入本文件。
# 用法：pip install "ruff==0.16.3" && ruff check . && ruff format --check .
